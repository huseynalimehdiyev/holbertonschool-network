#!/usr/bin/env bash
# Configures localhost and facebook.com IP addresses.

sed -i 's/^127\.0\.0\.1[[:space:]]\+localhost/127.0.0.2 localhost/' /etc/hosts
sed -i '/^[0-9.]*[[:space:]]\+facebook\.com$/d' /etc/hosts
echo "8.8.8.8 facebook.com" >> /etc/hosts